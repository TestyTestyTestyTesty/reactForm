export default function SuggestionList({suggestions}) {
    console.log(suggestions);
/*     const suggestionList = suggestions.map(item => {
        <li key={item.cca3}>{item.common.name}</li>
    }) */
  return (
    <p>asd</p>
  )
}
